{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Wish List{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>

    {% for wish in wishes %}
        <p><a href="{{ path('app_wish_detail', {'id': wish.id}) }}">{{ wish.title }}</a></p>
    {% endfor %}
{% endblock %}
