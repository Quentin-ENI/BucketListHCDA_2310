{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    {{ form_start(formWish) }}
        {{ form_widget(formWish.thumbnail) }}
    {{ form_end(formWish) }}
{% endblock %}
